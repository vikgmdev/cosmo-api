import app from './app';
import auth from './auth';
import custom from './custom';
import database from './database';
import jwt from './jwt';

export const Config = {
  app,
  auth,
  custom,
  database,
  jwt,
};
